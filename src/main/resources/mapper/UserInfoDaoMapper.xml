<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ch.demo.dao.UserInfoDao">

    <select id="findAllUser" resultType="com.ch.demo.entity.UserInfo">
        select * from user_info
    </select>

    <select id="getUserById" resultType="com.ch.demo.entity.UserInfo">
        select * from user_info where id = #{id}
    </select>

    <select id="getUserByIdEqualOne" resultType="com.ch.demo.entity.UserInfo">
        select * from user_info where id =1
    </select>

    <select id="getUserByIdEqualTwo" resultType="com.ch.demo.entity.UserInfo">
        select * from user_info where id =2
    </select>
</mapper>